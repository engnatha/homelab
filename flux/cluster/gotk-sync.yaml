apiVersion: source.toolkit.fluxcd.io/v1
kind: GitRepository
metadata:
  name: homelab-repo
  namespace: flux-system
spec:
  interval: 5m0s
  url: ssh://git@github.com/engnatha/homelab
  secretRef:
    name: homelab-repo
  ref:
    branch: main
